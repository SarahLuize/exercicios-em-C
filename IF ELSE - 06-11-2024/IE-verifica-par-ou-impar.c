#include <stdio.h>
#include <stdlib.h>

int main()
{
    int numero;

    printf("Digite um numero\n");
    scanf("%d", &numero);

    resto = numero%2;

   if (num % 2 == 0) {
        printf("%d : PAR", numero);
    }
    else {
        printf("%d : IMPAR", numero);
    }
    return 0;
}
